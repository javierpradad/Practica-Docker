services:
  modelo:
    build: ./model
    container_name: Modelo
    ports:
      - "5000:5000"
  gui:
    build: ./gui
    container_name: GUI
    ports:
      - "8080:8080"
    depends_on:
      - modelo