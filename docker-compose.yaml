version: '3'
services:
  Modelo:
    build: ./App
    container_name: Modelo
    ports:
      - "5000:5000"
  GUI:
    build: ./GUI
    container_name: GUI
    ports:
      - "8080:8080"
    depends_on:
      - Modelo